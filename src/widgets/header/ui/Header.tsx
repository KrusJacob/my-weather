import styles from "./styles.module.css";

const Header = () => {
  return (
    <header className={styles.header}>
      <div>
        <h2>My Weather</h2>
      </div>
    </header>
  );
};

export default Header;
